import pytest


@pytest.mark.skip(reason="Интеграция с Python‑интерпретатором системы ещё не реализована")
def test_python_interpritator_basic():
    """
    Черновой тест для встроенного Python‑интерпретатора.

    Идея:
    - через системный API/механику, которой вы вызываете Python из Elixir,
      выполнить небольшой набор выражений:
        * арифметика (1+1, 2**10),
        * работа со строками/словарями/списками,
        * json.dumps / json.loads,
        * намеренная ошибка (1/0) и проверка формата ошибки.
    - проверить, что ответ интерпретатора сериализуется и правильно
      возвращается в систему.

    Сейчас тест помечен skip и служит точкой входа: позже сюда
    добавим реальные вызовы вашей системы.
    """
    # TODO: когда появится публичный API для вызова Python‑интерпретатора,
    #       заменить этот заглушечный тест на реальные проверки.
    assert True


@pytest.mark.skip(reason="Нагрузочное тестирование интерпретатора ещё не реализовано")
def test_python_interpritator_under_load():
    """
    Черновой тест для нагрузочной проверки Python‑интерпретатора.

    План:
    - через систему сделать N параллельных вызовов интерпретатора,
      выполняющих одну и ту же CPU/память‑нагружающую функцию;
    - собрать статистику по времени ответа и количеству ошибок;
    - убедиться, что под нагрузкой нет таймаутов/500/502 и утечек.

    Конкретная реализация появится после согласования формата вызова
    интерпретатора из Elixir.
    """
    # TODO: реализовать нагрузочный сценарий через системный API.
    assert True


